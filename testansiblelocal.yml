---
  - name: "test case"
    hosts: localhost
    connection: local 
    vars: 
        string0:  It's working as expected

    tasks:

    - name: "echo special str0"
      shell: echo "{{ string0 }}"
      register: "output"
    - debug: var=output.stdout_lines

